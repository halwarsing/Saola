export enum AuthMode {
  RESET_PASSWORD = 'RESET_PASSWORD',
  SIGN_IN = 'SIGN_IN',
  SIGN_UP = 'SIGN_UP'
}

export enum InfoBlockType {
  NEWS = 'news',
  EVENT = 'event',
  LIFEHACK = 'lifehack'
}

export enum SizeType {
  SM = 'sm',
  MD = 'md',
  LG = 'lg'
}
